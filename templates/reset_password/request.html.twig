{% extends 'base.html.twig' %}

{% block title %}Changer votre mot de passe{% endblock %}

{% block content %}
    <div class="container">
        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}
        <h1>Changer votre mot de passe</h1>

        {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}
            <div>
                <small>
                    Entrez votre email et nous vous fourniront un lien pour changer votre mot de passe.
                </small>
            </div>

            <button class="btn btn-primary">Envoyez votre demande de nouveau mot de passe.</button>
        {{ form_end(requestForm) }}
    </div>
{% endblock %}
